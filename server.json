{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.onenomad-llc/engram-mcp",
  "description": "przm Memory: memory MCP server for Claude Code. LLM-powered extraction, hybrid ANN search, tier lifecycle, spreading activation, scoped procedural rules, inferred recall outcomes, session-start context, WAL, and session state.",
  "repository": {
    "url": "https://github.com/OneNomad-LLC/przm-memory",
    "source": "github"
  },
  "version": "1.4.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@onenomad/przm-memory",
      "version": "1.4.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OPENROUTER_API_KEY",
          "description": "OpenRouter API key for LLM-powered extraction and re-ranking. Any model on openrouter.ai works. Without this, extraction/re-rank features are disabled.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "PRZM_MEMORY_DATA_DIR",
          "description": "Directory for the LanceDB store and WAL. Defaults to ~/.claude/przm-memory. ENGRAM_DATA_DIR is still honoured.",
          "isRequired": false,
          "isSecret": false,
          "format": "filepath"
        }
      ]
    }
  ]
}
